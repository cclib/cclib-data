This directory will contain log files for Turbomole regression tests. 
This REAME file can be deleted once there are some log files in this directory
or subdirectories, but for now it is needed to add the directory to the git repo
 otherwise regression.py fails. 
